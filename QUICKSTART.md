# 🚀 Быстрый старт Dialog Keeper

## 1. Клонирование и настройка проекта

```bash
# Репозиторий уже склонирован в:
cd /home/nil/dialog-keeper
```

## 2. Создание защищённой папки для токенов

```bash
# Создайте папку secrets (защищена от AI и Git)
mkdir -p secrets
chmod 700 secrets

# Эта папка НЕ будет видна AI ассистенту и не попадёт в Git!
```

## 3. Настройка переменных окружения

```bash
# Создайте .env из шаблона
cp .env.example .env

# Отредактируйте .env и добавьте реальные токены
nano .env
```

### Минимальные токены для запуска:

```bash
# В файле .env укажите:
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
OPENAI_API_KEY=ваш_ключ_OpenAI
DATABASE_URL=sqlite:///./dialog_keeper.db
```

## 4. Получение токенов

### Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен в `.env`

### OpenAI API Key

1. Зайдите на [platform.openai.com/api-keys](https://platform.openai.com/api-keys)
2. **Create new secret key**
3. Скопируйте ключ в `.env`

### Kaiten API Token (опционально)

1. Войдите в Kaiten
2. **Настройки** → **API** → **Создать токен**
3. Скопируйте токен в `.env`

## 5. Установка зависимостей

```bash
# Python 3.11+ рекомендуется
python3 --version

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости (когда будет requirements.txt)
# pip install -r requirements.txt

# Или с Poetry
# poetry install
```

## 6. Запуск бота (пример)

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите бота
python src/main.py

# Или с Poetry
# poetry run python src/main.py
```

## 7. Проверка защиты

```bash
# Создайте тестовый секрет
echo "TEST=secret123" > secrets/test.txt

# Файл защищён - AI не сможет его прочитать!
cat secrets/test.txt  # Вы увидите
# AI не увидит содержимое этого файла
```

## 📁 Структура проекта

```
dialog-keeper/
├── secrets/              # ← Ваши токены (создайте вручную, защищено)
├── .env                  # ← Конфигурация (создайте из .env.example)
├── .env.example          # ← Шаблон
├── .cursorignore         # ← Защита от AI
├── .gitignore            # ← Защита от Git
├── SECURITY.md           # ← Подробная документация по безопасности
├── QUICKSTART.md         # ← Этот файл
├── README.md             # ← Описание проекта
└── src/                  # ← Код (создайте структуру)
    ├── main.py
    ├── bot/
    ├── ai/
    └── integrations/
```

## ⚡ Следующие шаги

1. **Создайте структуру проекта:**

```bash
mkdir -p src/{bot,ai,integrations,database}
mkdir -p data logs
touch src/main.py
```

2. **Добавьте зависимости** (requirements.txt или pyproject.toml):

```txt
python-telegram-bot>=20.0
openai>=1.0
python-dotenv>=1.0
sqlalchemy>=2.0
qdrant-client>=1.0
```

3. **Настройте базу данных:**

```bash
# SQLite для разработки (по умолчанию)
# Файл создастся автоматически при первом запуске
```

4. **Прочитайте документацию:**
   - `SECURITY.md` — детали по безопасности
   - `README.md` — описание проекта
   - [Telegram Bot API](https://core.telegram.org/bots/api)
   - [OpenAI API](https://platform.openai.com/docs)

## 🔒 Безопасность

- ✅ Папка `secrets/` защищена от AI и Git
- ✅ `.env` защищён от AI и Git
- ✅ Токены не попадут в репозиторий
- ✅ AI ассистент не видит чувствительные данные

## 🆘 Помощь

Если что-то не работает:

1. Проверьте `.env` — все токены заполнены?
2. Проверьте права доступа: `ls -la secrets/`
3. Проверьте логи: `tail -f logs/dialog_keeper.log`
4. Прочитайте `SECURITY.md` для деталей

## ✅ Чек-лист первого запуска

- [ ] Создана папка `secrets/` с правами 700
- [ ] Создан файл `.env` из `.env.example`
- [ ] Получен Telegram Bot Token от @BotFather
- [ ] Получен OpenAI API Key
- [ ] Токены добавлены в `.env`
- [ ] Установлено виртуальное окружение Python
- [ ] Установлены зависимости
- [ ] Проект запускается без ошибок

---

**Готово!** Теперь можно начинать разработку 🎉

Подробная документация: [SECURITY.md](./SECURITY.md)

