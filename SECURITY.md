# 🔒 Безопасность Dialog Keeper

## Структура защищённых данных

В этом проекте настроена многоуровневая защита чувствительных данных от AI ассистента, Git и несанкционированного доступа.

## 📁 Папка `secrets/` — Хранилище токенов

Эта папка **защищена** и не доступна для:
- ✅ AI ассистента Cursor (через `.cursorignore`)
- ✅ Git репозитория (через `.gitignore`)
- ✅ Внешнего мира (права доступа 700)

### Создание защищённой папки:

```bash
# Перейдите в директорию проекта
cd /home/nil/dialog-keeper

# Создайте папку для секретов
mkdir -p secrets
chmod 700 secrets  # Только владелец может читать

# Создайте файл с токенами
touch secrets/.env
chmod 600 secrets/.env  # Только владелец может читать/писать
```

## 🌍 Использование `.env` файла

### 1. Создайте локальный `.env`:

```bash
cp .env.example .env
nano .env  # Отредактируйте и заполните реальными значениями
```

### 2. Основные токены, которые нужно настроить:

```bash
# Telegram Bot Token (получить у @BotFather)
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

# OpenAI API Key (для AI ответов и RAG)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Kaiten API Token (для интеграции с service desk)
KAITEN_API_TOKEN=your_kaiten_api_token_here
KAITEN_API_URL=https://your-company.kaiten.ru/api/v1
```

### 3. Использование в коде:

**Python (с python-dotenv):**

```python
from dotenv import load_dotenv
import os

load_dotenv()

TELEGRAM_TOKEN = os.getenv('TELEGRAM_BOT_TOKEN')
OPENAI_KEY = os.getenv('OPENAI_API_KEY')
KAITEN_TOKEN = os.getenv('KAITEN_API_TOKEN')
```

**Установка python-dotenv:**

```bash
pip install python-dotenv
# или
poetry add python-dotenv
```

## 🔑 Получение токенов

### Telegram Bot Token

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен и добавьте в `.env`

```bash
echo "TELEGRAM_BOT_TOKEN=ваш_токен" >> .env
```

### OpenAI API Key

1. Зайдите на [platform.openai.com](https://platform.openai.com)
2. **API keys** → **Create new secret key**
3. Скопируйте ключ (он больше не будет показан!)
4. Добавьте в `.env`:

```bash
echo "OPENAI_API_KEY=sk-proj-xxxxx" >> .env
```

### Kaiten API Token

1. Войдите в ваш аккаунт Kaiten
2. **Настройки** → **API** → **Создать токен**
3. Скопируйте токен
4. Добавьте в `.env`:

```bash
echo "KAITEN_API_TOKEN=ваш_токен" >> .env
echo "KAITEN_API_URL=https://your-company.kaiten.ru/api/v1" >> .env
```

## 🛡️ Защищённые файлы и папки

AI ассистент **НЕ МОЖЕТ** читать:

- ✅ `secrets/` — ваша папка для токенов
- ✅ `.env` — переменные окружения
- ✅ `*.key`, `*.pem` — SSH ключи и сертификаты
- ✅ `credentials/` — учётные данные
- ✅ `private/` — личные файлы
- ✅ `*.db`, `*.sqlite` — базы данных
- ✅ `logs/*.log` — лог файлы
- ✅ `user_data/` — данные пользователей
- ✅ `sessions/` — сессии

## ✅ Проверка защиты

Создайте тестовый секрет и убедитесь, что AI не видит его:

```bash
mkdir -p secrets
echo "TEST_SECRET=12345" > secrets/test.txt
cat secrets/test.txt  # Вы увидите содержимое
```

Теперь спросите AI прочитать `secrets/test.txt` — он не сможет!

## 📦 Структура проекта (рекомендуемая)

```
dialog-keeper/
├── secrets/                 # ← Токены (создайте вручную)
│   ├── .env                # Основные секреты
│   ├── telegram_token.txt
│   ├── kaiten_token.txt
│   └── openai_key.txt
│
├── credentials/             # ← OAuth credentials (защищено)
│   ├── google_calendar.json
│   └── service_account.json
│
├── .env                     # ← Локальная конфигурация (из .env.example)
├── .env.example             # ← Шаблон (без реальных данных)
│
├── .cursorignore            # ← Защита от AI
├── .gitignore               # ← Защита от Git
│
├── src/                     # Код приложения
│   ├── bot/                # Telegram bot
│   ├── ai/                 # AI/RAG модуль
│   ├── integrations/       # Kaiten, календари
│   └── database/           # БД модели
│
├── data/                    # Данные (не коммитить)
│   ├── knowledge_base/     # RAG векторная БД
│   └── dialog_keeper.db    # SQLite БД
│
├── logs/                    # Логи (не коммитить)
│   └── *.log
│
├── requirements.txt         # Python зависимости
├── pyproject.toml          # Poetry конфиг (если используется)
├── README.md               # Описание проекта
└── SECURITY.md             # Этот файл
```

## 🚨 Безопасность в продакшене

### 1. Переменные окружения на сервере

**Используйте системные переменные окружения вместо .env:**

```bash
# В ~/.bashrc или systemd service file
export TELEGRAM_BOT_TOKEN="..."
export OPENAI_API_KEY="..."
export DATABASE_URL="postgresql://..."
```

### 2. Docker Secrets (если используете Docker)

```yaml
# docker-compose.yml
services:
  bot:
    image: dialog-keeper:latest
    secrets:
      - telegram_token
      - openai_key
      - kaiten_token

secrets:
  telegram_token:
    file: ./secrets/telegram_token.txt
  openai_key:
    file: ./secrets/openai_key.txt
  kaiten_token:
    file: ./secrets/kaiten_token.txt
```

### 3. Vault (Hashicorp) для enterprise

```bash
# Получение секретов из Vault
vault kv get -field=token secret/telegram/bot
```

## 📋 Чек-лист безопасности

- [ ] Создана папка `secrets/` с правами 700
- [ ] Создан `.env` файл из `.env.example`
- [ ] Все токены добавлены в `.env`
- [ ] `.env` добавлен в `.gitignore` и `.cursorignore`
- [ ] GitHub не содержит реальных токенов (проверьте историю!)
- [ ] Telegram Bot Token имеет ограничения (только нужные разрешения)
- [ ] OpenAI API Key имеет rate limits
- [ ] Kaiten Token создан с минимальными правами
- [ ] Включено логирование доступа к секретам
- [ ] Настроен мониторинг использования API

## 🆘 Что делать, если токен утёк?

### 1. **Немедленно отзовите токен!**

**Telegram Bot:**
```
@BotFather → /revoke → выберите бота → /newtoken
```

**OpenAI:**
```
platform.openai.com → API keys → Revoke → Create new
```

**Kaiten:**
```
Kaiten → Настройки → API → Удалить токен → Создать новый
```

### 2. **Удалите токен из Git истории** (если попал)

```bash
# ОСТОРОЖНО! Перепишет историю Git
git filter-branch --force --index-filter \
  "git rm --cached --ignore-unmatch .env secrets/*" \
  --prune-empty --tag-name-filter cat -- --all

# Force push (если репозиторий приватный)
git push --force --all
```

### 3. **Проверьте логи использования**

- OpenAI: [platform.openai.com/usage](https://platform.openai.com/usage)
- Telegram: Проверьте активность бота
- Kaiten: Проверьте audit log

### 4. **Обновите все .env файлы**

```bash
# На всех серверах/разработчиках
nano .env  # Обновите токены
systemctl restart dialog-keeper  # Перезапустите сервис
```

## 🔐 Рекомендации по безопасности

1. **Используйте разные токены для dev/staging/prod**
2. **Ротация токенов каждые 3-6 месяцев**
3. **Минимальные привилегии** (принцип least privilege)
4. **Мониторинг необычной активности** API
5. **Шифрование базы данных** с user_data
6. **Backup секретов** в защищённом хранилище (1Password, Bitwarden)
7. **2FA везде**, где возможно
8. **Аудит доступа** к серверам и репозиториям

## 📞 Контакты

При обнаружении уязвимостей:
- 📧 Email: security@your-company.com
- 🔒 Responsible Disclosure: [security.txt](/.well-known/security.txt)

---

**Статус:** ✅ Защита активирована  
**Последнее обновление:** 2026-01-08  
**Версия:** 1.0

---

## 📚 Дополнительные ресурсы

- [OWASP API Security Top 10](https://owasp.org/www-project-api-security/)
- [Telegram Bot Security Best Practices](https://core.telegram.org/bots/security)
- [OpenAI Safety Best Practices](https://platform.openai.com/docs/guides/safety-best-practices)
- [Python Security Best Practices](https://python.readthedocs.io/en/latest/library/security.html)

