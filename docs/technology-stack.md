# 🛠️ Выбор технологического стека Dialog Keeper

## Введение

Данный документ содержит анализ возможных технологий, фреймворков и подходов для реализации Dialog Keeper, с обоснованием выбора и сравнением альтернатив.

---

## 1. Telegram Bot Framework

### 1.1. Python: python-telegram-bot ✅ **РЕКОМЕНДУЕТСЯ**

**Описание:** Популярная асинхронная библиотека для Telegram Bot API на Python.

**Плюсы:**
- ✅ Полная поддержка Telegram Bot API 7.0+
- ✅ Async/await из коробки (высокая производительность)
- ✅ Активное комьюнити и отличная документация
- ✅ Встроенная поддержка webhook и long polling
- ✅ Богатая экосистема (интеграция с FastAPI, SQLAlchemy и т.д.)
- ✅ Type hints и mypy-friendly

**Минусы:**
- ⚠️ Требует понимания async/await в Python
- ⚠️ Версия 20+ несовместима с 13.x (breaking changes)

**Альтернативы:**

#### aiogram
**Плюсы:**
- Более лёгкий и быстрый
- FSM (Finite State Machine) из коробки
- Встроенный роутинг

**Минусы:**
- Меньше комьюнити
- Менее гибкий для сложных сценариев

**Вердикт:** `python-telegram-bot` предпочтительнее для enterprise-проекта благодаря зрелости и комьюнити.

#### Telebot (pyTelegramBotAPI)
**Плюсы:**
- Простой API
- Синхронный (проще для начинающих)

**Минусы:**
- ❌ Синхронный (низкая производительность при масштабировании)
- ❌ Менее активная разработка

**Вердикт:** Не подходит для высоконагруженных систем.

---

### 1.2. Node.js: node-telegram-bot-api / Telegraf

**Плюсы:**
- Высокая производительность благодаря event loop
- Большая экосистема npm пакетов
- Хорошо подходит для микросервисов

**Минусы:**
- ❌ Python экосистема для AI/ML гораздо богаче (LangChain, OpenAI SDK, etc.)
- ❌ Type safety хуже (даже с TypeScript)
- ❌ Callback hell при сложной логике

**Вердикт:** Python предпочтительнее для проекта с интенсивным использованием AI/ML.

---

## 2. AI/ML Stack

### 2.1. LLM Providers

#### OpenAI GPT-4 ✅ **РЕКОМЕНДУЕТСЯ**

**Модель:** `gpt-4o` или `gpt-4o-mini`

**Плюсы:**
- ✅ Лучшее качество генерации на сегодня
- ✅ Стабильный API с высоким SLA
- ✅ Встроенная поддержка function calling
- ✅ Хорошая документация и SDK

**Минусы:**
- ⚠️ Стоимость: $0.03/$0.06 за 1K tokens (input/output) для GPT-4
- ⚠️ Зависимость от внешнего сервиса
- ⚠️ Потенциальные санкции (хотя обычно работает)

**Цена (примерная):**
- 1000 запросов/день × 500 tokens = 500K tokens/день
- ~$15-30/день = $450-900/месяц

#### Anthropic Claude 3.5 Sonnet ✅ **АЛЬТЕРНАТИВА**

**Плюсы:**
- ✅ Качество на уровне GPT-4
- ✅ Больший контекстное окно (200K tokens)
- ✅ Лучше следует инструкциям (меньше hallucinations)
- ✅ Более дешёвый: $0.003/$0.015 за 1K tokens

**Минусы:**
- ⚠️ API менее стабильный (более молодой)
- ⚠️ Меньше сторонних интеграций

**Вердикт:** Хороший выбор как основной или запасной провайдер.

#### Локальные модели (Llama 3.2, Mistral, Qwen)

**Плюсы:**
- ✅ Нет зависимости от внешних API
- ✅ Нет recurring costs (после инфраструктуры)
- ✅ Полный контроль над данными

**Минусы:**
- ❌ Требуется мощное железо (GPU с ≥24GB VRAM для 70B моделей)
- ❌ Качество ниже, чем у GPT-4/Claude
- ❌ Сложнее масштабировать
- ❌ Требуется экспертиза в fine-tuning

**Вердикт:** Рассмотреть для будущего, но начать с OpenAI/Anthropic.

---

### 2.2. Embeddings (Векторизация)

#### OpenAI text-embedding-3-small ✅ **РЕКОМЕНДУЕТСЯ**

**Параметры:**
- Размерность: 1536 dimensions
- Стоимость: $0.00002 за 1K tokens
- Качество: Отличное для русского и английского

**Плюсы:**
- ✅ Высокое качество embeddings
- ✅ Дешёвый (100K документов ≈ $2)
- ✅ Простая интеграция с OpenAI API

**Минусы:**
- ⚠️ Зависимость от OpenAI

#### Sentence-Transformers (локальные модели)

**Модели:**
- `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
- `intfloat/multilingual-e5-large`

**Плюсы:**
- ✅ Бесплатно
- ✅ Работает локально
- ✅ Поддержка русского языка

**Минусы:**
- ⚠️ Качество чуть ниже OpenAI
- ⚠️ Требуется GPU для быстрой работы

**Вердикт:** OpenAI для production, Sentence-Transformers как backup.

---

### 2.3. RAG Framework

#### LangChain ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ De-facto стандарт для RAG в Python
- ✅ Огромная экосистема интеграций (OpenAI, Qdrant, Pinecone, etc.)
- ✅ Готовые компоненты: retrievers, chains, agents
- ✅ Активная разработка

**Минусы:**
- ⚠️ Иногда избыточен для простых задач
- ⚠️ Быстрые breaking changes между версиями
- ⚠️ Может быть "магическим" (сложно дебажить)

**Альтернатива: Llama Index**

**Плюсы:**
- Более focused на RAG (меньше overhead)
- Хорошая документация

**Минусы:**
- Меньше интеграций
- Меньше комьюнити

**Альтернатива: Haystack**

**Плюсы:**
- Production-ready из коробки
- Хорошая для enterprise

**Минусы:**
- Тяжеловесный
- Сложнее кастомизировать

**Вердикт:** **LangChain** для гибкости, или **своя реализация RAG** (если нужна полная кастомизация).

---

## 3. Vector Database

### 3.1. Qdrant ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ Open-source (Apache 2.0)
- ✅ Написан на Rust (быстрый и надёжный)
- ✅ Отличная Python SDK
- ✅ Встроенная фильтрация по метаданным
- ✅ Легко self-host (Docker)
- ✅ Есть managed cloud (qdrant.tech)

**Минусы:**
- ⚠️ Относительно молодой проект (но активно развивается)

**Цена:**
- Self-hosted: бесплатно (только инфраструктура)
- Cloud: от $25/месяц

### 3.2. ChromaDB

**Плюсы:**
- Очень простой API
- Встроенный режим (SQLite-like)
- Хорош для прототипирования

**Минусы:**
- ⚠️ Менее production-ready
- ⚠️ Производительность ниже при больших объёмах

**Вердикт:** Хорош для MVP, но Qdrant лучше для production.

### 3.3. Pinecone

**Плюсы:**
- Полностью managed
- Высокая производительность
- Хорошая документация

**Минусы:**
- ❌ Проприетарный (vendor lock-in)
- ❌ Дорогой: от $70/месяц
- ❌ Нет self-hosted варианта

**Вердикт:** Избегать из-за vendor lock-in.

### 3.4. PostgreSQL + pgvector

**Плюсы:**
- Всё в одной БД (меньше moving parts)
- Простая интеграция с существующим PostgreSQL

**Минусы:**
- ⚠️ Производительность хуже специализированных vector DB
- ⚠️ Ограниченные возможности фильтрации

**Вердикт:** Хорош для маленьких объёмов (<100K векторов), иначе Qdrant.

---

## 4. Speech-to-Text

### 4.1. OpenAI Whisper API ✅ **РЕКОМЕНДУЕТСЯ ДЛЯ MVP**

**Плюсы:**
- ✅ Отличное качество (особенно для русского)
- ✅ Простой API
- ✅ Автоопределение языка
- ✅ Дешёвый: $0.006 за минуту

**Минусы:**
- ⚠️ Latency: 5-15 секунд (зависит от длины)
- ⚠️ Зависимость от OpenAI

**Цена:**
- 100 голосовых сообщений/день × 30 сек = 50 минут/день
- ~$0.30/день = $9/месяц

### 4.2. Локальный Whisper (openai-whisper)

**Модели:**
- `tiny` (39M) — быстро, но низкое качество
- `base` (74M) — баланс
- `small` (244M) — хорошее качество
- `medium` (769M) — отличное качество
- `large-v3` (1550M) — лучшее качество

**Плюсы:**
- ✅ Бесплатно (после железа)
- ✅ Нет зависимости от API
- ✅ Полный контроль

**Минусы:**
- ⚠️ Требуется GPU (хотя бы 4GB VRAM для `base`)
- ⚠️ Сложнее масштабировать
- ⚠️ Нужно управлять инфраструктурой

**Вердикт:** Whisper API для MVP, локальный Whisper для production (если объёмы большие).

### 4.3. Yandex SpeechKit

**Плюсы:**
- Отличное качество для русского
- Hosted решение

**Минусы:**
- ❌ Дороже Whisper
- ❌ Vendor lock-in
- ❌ Проблемы с санкциями

**Вердикт:** Рассмотреть как альтернативу.

---

## 5. Database

### 5.1. PostgreSQL 16 ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ Проверенная, стабильная СУБД
- ✅ Отличная поддержка JSON, full-text search
- ✅ Расширение pgvector (для backup векторов)
- ✅ Высокая производительность
- ✅ Бесплатная и open-source

**Минусы:**
- ⚠️ Требует настройки для оптимальной производительности

**Альтернативы:**

#### MySQL/MariaDB
**Минусы:**
- ❌ Хуже поддержка JSON
- ❌ Менее богатая экосистема расширений

#### MongoDB
**Минусы:**
- ❌ Избыточен для структурированных данных
- ❌ Сложнее поддерживать ACID

**Вердикт:** PostgreSQL — золотой стандарт.

---

### 5.2. Redis 7 ✅ **РЕКОМЕНДУЕТСЯ**

**Назначение:**
- Кэширование (embeddings, responses)
- Message queue (Celery)
- Rate limiting
- Session storage

**Плюсы:**
- ✅ Очень быстрый (in-memory)
- ✅ Простой API
- ✅ Надёжный

**Альтернативы:**

#### Memcached
**Минусы:**
- ❌ Менее функциональный (только кэш)
- ❌ Нет персистентности

#### RabbitMQ (для очередей)
**Плюсы:**
- Более надёжный для critical queues

**Минусы:**
- ⚠️ Тяжеловесней
- ⚠️ Сложнее настройка

**Вердикт:** Redis достаточно для проекта.

---

## 6. Task Queue

### 6.1. Celery ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ Стандарт для Python
- ✅ Хорошая интеграция с Flask/FastAPI
- ✅ Встроенные retry, scheduling
- ✅ Flower для мониторинга

**Минусы:**
- ⚠️ Немного тяжеловесный для простых задач

**Альтернативы:**

#### RQ (Redis Queue)
**Плюсы:**
- Проще и легковесней Celery

**Минусы:**
- ⚠️ Менее функциональный

#### Dramatiq
**Плюсы:**
- Современный, легковесный

**Минусы:**
- ⚠️ Меньше комьюнити

**Вердикт:** Celery для production, RQ для MVP (если хочется быстро).

---

## 7. Workflow Automation

### 7.1. n8n ✅ **РЕКОМЕНДУЕТСЯ**

**Описание:** Open-source платформа для автоматизации workflow с визуальным редактором (low-code). Аналог Zapier/Make, но self-hosted.

**Назначение в Dialog Keeper:**
- **Оркестрация интеграций** — визуальное управление потоками данных между сервисами
- **Автоматизация рутинных задач** — создание workflow без написания кода
- **Webhook endpoints** — приём событий от внешних систем
- **Scheduled tasks** — периодические задачи (синхронизация, backup, отчёты)
- **Error handling** — retry логика, fallback, алерты
- **Rapid prototyping** — быстрое тестирование новых интеграций

**Плюсы:**
- ✅ **Визуальный редактор** — понятно без кода, легко дебажить
- ✅ **350+ готовых интеграций** (Telegram, Google, Kaiten-like сервисы, календари, AI)
- ✅ **Self-hosted** — полный контроль над данными, бесплатно
- ✅ **Webhook support** — может принимать события от Telegram, Kaiten, etc.
- ✅ **Code nodes** — можно писать кастомную логику на JavaScript/Python
- ✅ **REST API** — можно управлять workflow программно
- ✅ **Credential management** — безопасное хранение токенов и API ключей
- ✅ **Execution logs** — детальная история выполнения workflow
- ✅ **Active community** — много готовых шаблонов и поддержки

**Минусы:**
- ⚠️ Требует отдельный инстанс (Docker контейнер)
- ⚠️ Визуальные workflow могут стать сложными при масштабировании
- ⚠️ JavaScript/Python в code nodes — ограниченная функциональность

**Применение в Dialog Keeper:**

#### 1. Интеграция с Kaiten
```
Workflow: "Create Kaiten Ticket on Escalation"
Trigger: Webhook (от Dialog Keeper бота)
Steps:
  1. Receive escalation data
  2. Format ticket description
  3. Call Kaiten API → Create ticket
  4. Send response back to bot
  5. Log to database
  6. Send notification to Telegram channel
```

#### 2. Синхронизация базы знаний
```
Workflow: "Sync Kaiten Resolved Tickets to Knowledge Base"
Trigger: Schedule (каждые 6 часов)
Steps:
  1. Fetch closed tickets from Kaiten (last 6h)
  2. Extract question + resolution
  3. Generate embeddings (OpenAI API)
  4. Save to Qdrant vector DB
  5. Update PostgreSQL metadata
  6. Send summary to admin channel
```

#### 3. Календарь и встречи
```
Workflow: "Schedule Meeting Request"
Trigger: Webhook (определено намерение "schedule_meeting")
Steps:
  1. Parse meeting request (date, time, participants)
  2. Check Google Calendar free/busy
  3. Propose available slots
  4. Create calendar event on confirmation
  5. Send invites to participants
  6. Notify in Telegram chat
```

#### 4. Мониторинг и алерты
```
Workflow: "Health Check & Alerts"
Trigger: Schedule (каждые 5 минут)
Steps:
  1. Check bot status (last message timestamp)
  2. Check database connectivity
  3. Check OpenAI API quota
  4. Check Qdrant health
  5. If error → Send alert to admin Telegram
  6. Log metrics to Prometheus
```

#### 5. Ночной отчёт
```
Workflow: "Daily Summary Report"
Trigger: Schedule (23:00 каждый день)
Steps:
  1. Query PostgreSQL → stats (messages, AI answers, escalations)
  2. Generate charts (Chart.js API)
  3. Format summary message
  4. Send to team Telegram channel
```

**Альтернативы:**

#### Zapier / Make (Integromat)
**Плюсы:**
- Hosted решение (не нужно управлять инфраструктурой)
- Больше готовых интеграций

**Минусы:**
- ❌ Дорого ($20-100+/месяц)
- ❌ Нет self-hosted варианта
- ❌ Vendor lock-in
- ❌ Данные проходят через их серверы

#### Airflow (Apache)
**Плюсы:**
- Мощный для сложных data pipelines
- Python-based (больше контроля)

**Минусы:**
- ❌ Тяжеловесный (нужны ресурсы)
- ❌ Сложнее настройка и поддержка
- ❌ Нет визуального редактора (только код)

#### Prefect / Dagster
**Плюсы:**
- Современные альтернативы Airflow
- Лучше developer experience

**Минусы:**
- ⚠️ Больше для data engineering, чем для бизнес-автоматизации
- ⚠️ Нет готовых интеграций с бизнес-приложениями

#### Просто Celery + Python код
**Плюсы:**
- Полный контроль
- Нет новых зависимостей

**Минусы:**
- ❌ Каждую интеграцию нужно кодить вручную
- ❌ Сложнее итерироваться (изменения требуют деплоя)
- ❌ Нет визуального представления workflow
- ❌ Сложнее для non-developers (например, для настройки бизнес-логики)

**Вердикт:** 

**n8n — идеальный баланс** для Dialog Keeper:
- ✅ **Быстрая разработка интеграций** без написания кода
- ✅ **Визуальное отображение** всех автоматизаций
- ✅ **Self-hosted** (контроль + бесплатно)
- ✅ **Гибкость** — можно добавить код, где нужно
- ✅ **Low barrier** для команды (не только разработчики могут настраивать)

**Использовать n8n для:**
- ✅ Интеграции с внешними API (Kaiten, календари, CRM)
- ✅ Scheduled tasks (синхронизации, отчёты)
- ✅ Webhook endpoints
- ✅ Быстрое прототипирование новых фич

**Использовать Python/Celery для:**
- ✅ Критическая бизнес-логика (классификация, RAG)
- ✅ Высоконагруженные операции
- ✅ Сложная логика с требованиями к производительности

---

## 8. REST API (опционально)

### 8.1. FastAPI ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ Async из коробки (высокая производительность)
- ✅ Автоматическая генерация OpenAPI docs
- ✅ Type hints и Pydantic validation
- ✅ Современный и быстро развивающийся

**Минусы:**
- ⚠️ Относительно молодой (но уже mature)

**Альтернативы:**

#### Flask
**Плюсы:**
- Проверенный временем
- Огромная экосистема

**Минусы:**
- ❌ Синхронный (ниже производительность)
- ❌ Нет автодокументации из коробки

#### Django REST Framework
**Минусы:**
- ❌ Тяжеловесный для простого API
- ❌ Больше boilerplate

**Вердикт:** FastAPI если нужен REST API, иначе можно обойтись без него.

---

## 9. Containerization & Orchestration

### 9.1. Docker ✅ **ОБЯЗАТЕЛЬНО**

**Плюсы:**
- ✅ Воспроизводимая среда
- ✅ Лёгкий деплой
- ✅ Изоляция зависимостей

**Нет альтернатив:** Docker стал стандартом.

### 9.2. Docker Compose ✅ **ДЛЯ DEV И SMALL PROD**

**Плюсы:**
- ✅ Простой multi-container setup
- ✅ Хорош для разработки и маленьких деплоев

**Минусы:**
- ⚠️ Не для high-availability продакшена

### 9.3. Kubernetes 🔶 **ОПЦИОНАЛЬНО ДЛЯ LARGE SCALE**

**Когда нужен:**
- Высоконагруженная система (>10K запросов/час)
- Нужен auto-scaling
- Multi-region deployment

**Когда НЕ нужен:**
- Маленькая/средняя нагрузка
- Команда без DevOps экспертизы

**Вердикт:** Docker Compose для начала, Kubernetes при необходимости.

---

## 10. CI/CD

### 10.1. GitHub Actions ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ Бесплатный для public repos
- ✅ Интеграция с GitHub
- ✅ Огромная marketplace actions
- ✅ Простая настройка

**Альтернативы:**

#### GitLab CI
**Плюсы:**
- Более мощный для сложных пайплайнов

**Минусы:**
- ⚠️ Требует GitLab (если проект на GitHub)

#### Jenkins
**Минусы:**
- ❌ Устаревший подход
- ❌ Сложная настройка

**Вердикт:** GitHub Actions если на GitHub, иначе GitLab CI.

---

## 11. Monitoring & Logging

### 11.1. Логирование

#### Sentry ✅ **РЕКОМЕНДУЕТСЯ ДЛЯ ОШИБОК**

**Плюсы:**
- ✅ Отличная визуализация ошибок
- ✅ Группировка и алерты
- ✅ Бесплатный tier (5K events/month)

#### Structured logging (structlog)

**Плюсы:**
- JSON-формат логов
- Легко парсить и анализировать

### 11.2. Метрики

#### Prometheus + Grafana ✅ **РЕКОМЕНДУЕТСЯ**

**Плюсы:**
- ✅ Стандарт для метрик
- ✅ Grafana — красивые дашборды
- ✅ Open-source

**Альтернативы:**

#### DataDog
**Минусы:**
- ❌ Дорогой ($15+/host/month)

**Вердикт:** Prometheus для self-hosted, DataDog если есть бюджет.

---

## 12. Итоговые рекомендации

### 🎯 Рекомендуемый стек для MVP

```yaml
Backend:
  Language: Python 3.11+
  Bot Framework: python-telegram-bot
  Web Framework: FastAPI (опционально)

AI/ML:
  LLM: OpenAI GPT-4o-mini
  Embeddings: OpenAI text-embedding-3-small
  RAG: LangChain
  Speech-to-Text: OpenAI Whisper API

Databases:
  Main DB: PostgreSQL 16
  Vector DB: Qdrant (self-hosted)
  Cache/Queue: Redis 7

Task Queue & Automation:
  Task Queue: Celery + Redis
  Workflow Automation: n8n (self-hosted)

Infrastructure:
  Containerization: Docker
  Orchestration: Docker Compose
  CI/CD: GitHub Actions

Monitoring:
  Errors: Sentry
  Metrics: Prometheus + Grafana
  Logging: structlog (JSON)

Integrations:
  Workflow Platform: n8n
  Kaiten: REST API (через n8n или напрямую)
  Calendar: Google Calendar API (через n8n)
```

### 💰 Ориентировочная стоимость (месяц)

| Компонент | Стоимость |
|-----------|-----------|
| OpenAI API (GPT-4-mini + Whisper + Embeddings) | $200-500 |
| VPS (20GB RAM, 6 CPU) для bot+n8n+postgres+qdrant+redis | $70-120 |
| n8n (self-hosted) | $0 |
| Qdrant (self-hosted) | $0 |
| Sentry (бесплатный tier) | $0 |
| Domain + SSL | $2-5 |
| **Итого:** | **$272-625/месяц** |

**Примечание:** n8n self-hosted бесплатен, но требует больше RAM (+4GB), поэтому VPS чуть дороже.

### 🚀 Путь миграции (если нужно удешевить)

**Phase 1 (MVP):** OpenAI API для всего  
**Phase 2:** Локальный Whisper (экономия ~$10/мес)  
**Phase 3:** Локальные embeddings (экономия ~$5/мес)  
**Phase 4:** Локальный LLM (экономия ~$200+/мес, но нужно GPU)

---

### 🏗️ Архитектура с n8n

```
┌─────────────────────────────────────────────────┐
│              Dialog Keeper Bot                  │
│              (python-telegram-bot)              │
└──────────────┬──────────────────────────────────┘
               │
         ┌─────┴──────┐
         │            │
         ▼            ▼
    ┌─────────┐  ┌─────────────┐
    │  Celery │  │     n8n     │◄── Webhook Endpoints
    │  Tasks  │  │  Workflows  │
    └─────────┘  └──────┬──────┘
         │              │
         │         ┌────┴─────┬──────────┬─────────┐
         │         ▼          ▼          ▼         ▼
         │    ┌────────┐ ┌────────┐ ┌───────┐ ┌──────┐
         │    │ Kaiten │ │Calendar│ │OpenAI │ │ CRM  │
         │    │  API   │ │  APIs  │ │ API   │ │ API  │
         │    └────────┘ └────────┘ └───────┘ └──────┘
         │
         ▼
    ┌──────────────────────────────┐
    │  PostgreSQL + Qdrant + Redis │
    └──────────────────────────────┘
```

**Разделение ответственности:**
- **Bot (Python):** Получение сообщений, AI/RAG, отправка ответов
- **Celery:** Тяжёлые задачи (транскрибация, embeddings, batch processing)
- **n8n:** Интеграции с внешними API, scheduled tasks, webhook processing

---

## 13. Критерии выбора: Summary

| Критерий | Вес | Выбор | Обоснование |
|----------|-----|-------|-------------|
| **Скорость разработки** | ⭐⭐⭐⭐⭐ | Python + LangChain | Богатая экосистема, быстрое прототипирование |
| **Стоимость эксплуатации** | ⭐⭐⭐⭐ | OpenAI API → Постепенно локальные модели | Начать с облака, мигрировать при росте |
| **Качество AI** | ⭐⭐⭐⭐⭐ | OpenAI GPT-4 | Лучшее качество на сегодня |
| **Масштабируемость** | ⭐⭐⭐⭐ | Qdrant + PostgreSQL + Redis | Хорошо масштабируется |
| **Vendor lock-in** | ⭐⭐⭐ | Избегать Pinecone, предпочесть Open-Source | Qdrant, PostgreSQL, n8n — open-source |
| **Поддержка команды** | ⭐⭐⭐⭐ | Python + Docker + n8n | Низкий порог входа для новых разработчиков |
| **Скорость итераций** | ⭐⭐⭐⭐⭐ | n8n для быстрого прототипирования | Интеграции без кода, визуальный редактор |

---

**Версия:** 1.0  
**Дата:** 2026-01-08  
**Статус:** Рекомендации → Ожидает утверждения  
**Компания:** РИПАС

