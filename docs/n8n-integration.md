# 🔄 Интеграция n8n в Dialog Keeper

## Введение

**n8n** — это open-source платформа для автоматизации workflow с визуальным редактором. В Dialog Keeper мы используем n8n для оркестрации интеграций с внешними сервисами и автоматизации рутинных задач.

---

## Зачем n8n в проекте?

### ✅ Преимущества использования n8n

1. **Быстрое прототипирование** — новые интеграции за минуты, без кода
2. **Визуальное отображение** — понятно всей команде, не только разработчикам
3. **Готовые коннекторы** — 350+ интеграций из коробки
4. **Webhook endpoints** — легко принимать события от внешних систем
5. **Error handling** — встроенные retry, fallback, алерты
6. **Гибкость** — можно добавить код (JavaScript/Python) где нужно
7. **Self-hosted** — контроль над данными + бесплатно

### 🎯 Что делаем через n8n vs Python

| Задача | Инструмент | Причина |
|--------|-----------|---------|
| Классификация сообщений | Python/Celery | Критическая бизнес-логика, нужна производительность |
| RAG генерация ответов | Python/LangChain | Сложная логика, tight integration с ботом |
| Создание тикета в Kaiten | n8n | Простая интеграция, легко менять |
| Синхронизация базы знаний | n8n | Scheduled task, визуальный workflow |
| Планирование встреч (Google Calendar) | n8n | Готовый коннектор, быстро настроить |
| Ночные отчёты | n8n | Scheduled, легко добавить новые метрики |
| Алерты в Telegram | n8n | Webhook endpoint, простая логика |

---

## Архитектура интеграции

```
┌──────────────────────────────────────────────────┐
│          Dialog Keeper Bot (Python)              │
│                                                   │
│  • Получение сообщений Telegram                  │
│  • Классификация (AI)                            │
│  • RAG генерация ответов                         │
│  • Отправка ответов                              │
└─────────────┬────────────────────────────────────┘
              │
              │ HTTP / Webhook
              │
              ▼
┌──────────────────────────────────────────────────┐
│                     n8n                          │
│                                                   │
│  Workflow 1: Создание тикета Kaiten              │
│  Workflow 2: Синхронизация базы знаний           │
│  Workflow 3: Планирование встреч                 │
│  Workflow 4: Ночные отчёты                       │
│  Workflow 5: Алерты и мониторинг                 │
└─────────────┬────────────────────────────────────┘
              │
              │ REST API
              │
      ┌───────┴───────┬──────────┬──────────┐
      ▼               ▼          ▼          ▼
┌──────────┐   ┌──────────┐  ┌──────┐  ┌──────┐
│  Kaiten  │   │ Calendar │  │OpenAI│  │Telegram│
│   API    │   │   APIs   │  │ API  │  │  API   │
└──────────┘   └──────────┘  └──────┘  └──────┘
```

---

## Примеры Workflow

### 1. Создание тикета в Kaiten при эскалации

**Триггер:** Webhook (вызов от Python бота)

**Входные данные:**
```json
{
  "chat_id": "123456",
  "user_id": "789",
  "username": "john_doe",
  "question": "Как настроить OAuth?",
  "ai_answer": "Для настройки OAuth...",
  "escalation_reason": "user_requested_human"
}
```

**Шаги workflow:**
1. **Webhook** — принять данные от бота
2. **Function** — форматировать описание тикета
3. **HTTP Request** — создать тикет через Kaiten API
   ```
   POST https://ripas.kaiten.ru/api/v1/cards
   Headers: Authorization: Bearer ${KAITEN_TOKEN}
   Body: {
     "board_id": 12345,
     "column_id": 67890,
     "title": "Вопрос от @john_doe: Как настроить OAuth?",
     "description": "...",
     "custom_fields": {
       "telegram_chat_id": "123456",
       "telegram_username": "@john_doe"
     }
   }
   ```
4. **Telegram** — уведомить канал команды
5. **HTTP Response** — вернуть ticket_id боту
6. **PostgreSQL** — сохранить связь message_id ↔ ticket_id

**Результат:**
- Тикет создан в Kaiten
- Команда уведомлена в Telegram
- Пользователь получил подтверждение

---

### 2. Синхронизация закрытых тикетов Kaiten → База знаний

**Триггер:** Schedule (каждые 6 часов)

**Шаги workflow:**
1. **Schedule** — запуск в 00:00, 06:00, 12:00, 18:00
2. **HTTP Request** — получить закрытые тикеты за последние 6 часов
   ```
   GET https://ripas.kaiten.ru/api/v1/cards?
       status=closed&
       updated_after=2026-01-08T12:00:00Z
   ```
3. **Loop over items** — для каждого тикета:
   - Извлечь вопрос (title) и решение (last comment)
   - Очистить от лишнего (HTML, форматирование)
4. **OpenAI** — сгенерировать embedding
   ```
   POST https://api.openai.com/v1/embeddings
   Body: {
     "model": "text-embedding-3-small",
     "input": "вопрос + решение"
   }
   ```
5. **HTTP Request** — сохранить в Qdrant
   ```
   POST http://qdrant:6333/collections/knowledge_base/points
   Body: {
     "points": [{
       "id": "kaiten_ticket_12345",
       "vector": [...],
       "payload": {
         "question": "...",
         "answer": "...",
         "source": "kaiten",
         "ticket_id": 12345,
         "validated": true
       }
     }]
   }
   ```
6. **PostgreSQL** — обновить метаданные в основной БД
7. **Telegram** — отправить summary в канал админов
   ```
   "✅ Синхронизация завершена:
   - Обработано тикетов: 15
   - Добавлено в базу знаний: 12
   - Пропущено (дубли): 3"
   ```

**Результат:**
- База знаний автоматически пополняется
- AI бот начнёт использовать новые знания

---

### 3. Планирование встречи через Google Calendar

**Триггер:** Webhook (от бота, определено намерение "schedule_meeting")

**Входные данные:**
```json
{
  "chat_id": "123456",
  "user": "john_doe",
  "message": "Давайте созвонимся на следующей неделе, удобно во вторник или среду",
  "participants": ["john@example.com", "manager@ripas.ru"]
}
```

**Шаги workflow:**
1. **Webhook** — принять запрос
2. **OpenAI** — извлечь structured data
   ```
   Промпт: "Extract meeting details from message"
   Результат: {
     "preferred_days": ["tuesday", "wednesday"],
     "duration_minutes": 60,
     "next_week": true
   }
   ```
3. **Google Calendar** — проверить free/busy для участников
4. **Function** — найти первый свободный слот
5. **Telegram** — предложить варианты пользователю
   ```
   "Доступные слоты для созвона:
   1️⃣ Вторник, 14 января, 14:00-15:00
   2️⃣ Среда, 15 января, 10:00-11:00
   3️⃣ Среда, 15 января, 16:00-17:00
   
   Выберите удобное время, ответив цифрой."
   ```
6. **Wait for user response** (хранить state в Redis)
7. **Google Calendar** — создать событие после подтверждения
8. **Google Calendar** — отправить invite участникам
9. **Telegram** — подтвердить создание встречи

**Результат:**
- Встреча запланирована без ручной работы
- Все участники получили invite

---

### 4. Ежедневный отчёт для команды

**Триггер:** Schedule (23:00 каждый день)

**Шаги workflow:**
1. **Schedule** — запуск в 23:00 МСК
2. **PostgreSQL** — запросить статистику за день
   ```sql
   SELECT 
     COUNT(*) as total_messages,
     SUM(CASE WHEN is_noise THEN 1 ELSE 0 END) as noise_filtered,
     SUM(CASE WHEN ai_answered THEN 1 ELSE 0 END) as ai_responses,
     SUM(CASE WHEN escalated THEN 1 ELSE 0 END) as escalations
   FROM messages
   WHERE created_at::date = CURRENT_DATE
   ```
3. **Function** — рассчитать метрики
   ```javascript
   const efficiency = (ai_responses / total_messages * 100).toFixed(1);
   const noise_ratio = (noise_filtered / total_messages * 100).toFixed(1);
   ```
4. **Chart.js API** (опционально) — сгенерировать графики
5. **Telegram** — отправить отчёт в канал команды
   ```
   📊 Отчёт за 08 января 2026
   
   📥 Всего сообщений: 456
   🗑️ Отфильтровано флуда: 312 (68.4%)
   🤖 AI ответов: 98 (21.5%)
   👤 Эскалаций к специалистам: 46 (10.1%)
   
   🎯 Эффективность AI: 68.1% (98 из 144 значимых)
   ⏱️ Средний response time: 3.2 сек
   
   📈 Топ-3 вопроса:
   1. "Как подключиться к API?" (12 раз)
   2. "Где документация?" (8 раз)
   3. "Как настроить OAuth?" (7 раз)
   ```

**Результат:**
- Команда видит ежедневную аналитику
- Можно отслеживать тренды

---

### 5. Мониторинг и алерты

**Триггер:** Schedule (каждые 5 минут)

**Шаги workflow:**
1. **Schedule** — запуск каждые 5 минут
2. **HTTP Request** — проверить health endpoint бота
   ```
   GET http://bot:8000/health
   ```
3. **IF** response не 200 или timeout:
   - **Telegram** → отправить алерт админам
   - **Increment counter** в Redis
   - **IF** counter > 3 → отправить critical alert
4. **PostgreSQL** — проверить последнее сообщение
   ```sql
   SELECT MAX(created_at) FROM messages
   ```
5. **IF** более 10 минут назад:
   - **Telegram** → "⚠️ Бот не получает сообщения!"
6. **OpenAI API** — проверить quota
7. **Qdrant** — проверить доступность
8. **Prometheus** — отправить метрики

**Результат:**
- Проактивные алерты при проблемах
- Минимальный downtime

---

## Установка и настройка

### 1. Запуск n8n через Docker Compose

n8n уже включён в `docker-compose.yml`:

```bash
docker-compose up -d n8n
```

Откройте в браузере: http://localhost:5678

### 2. Первая настройка

1. Войдите с учётными данными из `.env`:
   - Username: `admin` (или из `N8N_USER`)
   - Password: из `N8N_PASSWORD`

2. Настройте credentials:
   - **Kaiten API:** Bearer Token
   - **OpenAI:** API Key
   - **Google Calendar:** OAuth2
   - **Telegram:** Bot Token
   - **PostgreSQL:** Connection string

### 3. Импорт готовых workflow

```bash
# Скопировать workflow в контейнер
docker cp n8n_workflows/ dialog-keeper-n8n:/home/node/.n8n/workflows/

# Или импортировать через UI: Settings → Import from File
```

### 4. Настройка webhook endpoints

В боте Python:

```python
import httpx

async def escalate_to_kaiten(message_data: dict) -> dict:
    """Отправить эскалацию в n8n workflow"""
    async with httpx.AsyncClient() as client:
        response = await client.post(
            "http://n8n:5678/webhook/escalate",
            json=message_data,
            timeout=30.0
        )
        return response.json()
```

---

## Best Practices

### ✅ Что делать через n8n

- Интеграции с внешними API (Kaiten, Calendar, CRM)
- Scheduled tasks (синхронизации, отчёты, cleanup)
- Webhook endpoints для приёма событий
- Простые трансформации данных
- Rapid prototyping новых фич

### ❌ Что НЕ делать через n8n

- Критическую бизнес-логику (классификация, RAG)
- Высоконагруженные real-time операции
- Сложную обработку данных (лучше Python/Pandas)
- Логику, требующую unit-тестов
- Алгоритмы с высокими требованиями к производительности

### 🔒 Безопасность

- ✅ Все credentials хранить в n8n Credentials (encrypted)
- ✅ Использовать Basic Auth для доступа к UI
- ✅ Webhook endpoints защищать токенами
- ✅ Ограничить доступ к n8n только для внутренней сети
- ✅ Регулярно экспортировать backup workflow

### 📊 Мониторинг

- Включить execution logging в n8n
- Настроить алерты при ошибках workflow
- Мониторить время выполнения workflow
- Отслеживать использование ресурсов

---

## Структура папок

```
dialog-keeper/
├── n8n/
│   ├── workflows/              # Экспортированные workflow (JSON)
│   │   ├── escalate-kaiten.json
│   │   ├── sync-knowledge-base.json
│   │   ├── schedule-meeting.json
│   │   ├── daily-report.json
│   │   └── monitoring.json
│   ├── credentials/           # Шаблоны credentials (без реальных данных!)
│   │   └── credentials.example.json
│   └── README.md             # Документация по workflow
└── docker-compose.yml        # n8n сервис
```

---

## Полезные ссылки

- [n8n Documentation](https://docs.n8n.io/)
- [n8n Community](https://community.n8n.io/)
- [n8n Workflow Templates](https://n8n.io/workflows/)
- [n8n Docker Setup](https://docs.n8n.io/hosting/installation/docker/)

---

**Версия:** 1.0  
**Дата:** 2026-01-08  
**Компания:** РИПАС

