# 🏗️ Обзор архитектуры Dialog Keeper

## Общая схема

```
┌─────────────────────────────────────────────────────────────────┐
│                     EXTERNAL USERS                              │
│              (Клиенты, Партнёры, Подрядчики)                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │   TELEGRAM    │
                    │   PLATFORM    │
                    └───────┬───────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                    DIALOG KEEPER BOT                            │
├─────────────────────────────────────────────────────────────────┤
│  ┌────────────────────────────────────────────────────────┐    │
│  │              MESSAGE PROCESSING LAYER                   │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • Message Router         • Voice Transcription         │    │
│  │  • Context Analyzer       • Noise Filter                │    │
│  │  • Intent Classifier      • Message Archiver            │    │
│  └────────────────────────────────────────────────────────┘    │
│                            │                                     │
│                            ▼                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                   AI/RAG ENGINE                         │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • Vector Search (Qdrant)                               │    │
│  │  • Knowledge Base (embeddings)                          │    │
│  │  • LLM Provider (OpenAI/Anthropic)                      │    │
│  │  • Response Generation                                  │    │
│  │  • Context Enhancement                                  │    │
│  └────────────────────────────────────────────────────────┘    │
│                            │                                     │
│                            ▼                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              INTEGRATIONS LAYER                         │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • Kaiten Service Desk API                              │    │
│  │  • Calendar Integration (Google/Outlook)                │    │
│  │  • Task Trackers (Jira, Asana)                          │    │
│  │  • MTS Digital PBX API (Phone Calls)                    │    │
│  │  • Internal Team Notifications                          │    │
│  └────────────────────────────────────────────────────────┘    │
│                            │                                     │
│                            ▼                                     │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                 DATA STORAGE LAYER                      │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  • PostgreSQL (main DB)                                 │    │
│  │  • Qdrant (vector DB)                                   │    │
│  │  • Redis (cache & queues)                               │    │
│  │  • File Storage (archives)                              │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                            │
                            ▼
                ┌───────────────────────┐
                │   INTERNAL STAFF      │
                │  (через Telegram +    │
                │   Kaiten Dashboard)   │
                └───────────────────────┘
```

## Ключевые компоненты

### 1. **Telegram Bot Layer**
- **python-telegram-bot** - основной фреймворк
- Обработка webhook или long polling
- Управление сессиями и контекстом
- Rate limiting и защита от флуда

### 2. **Message Processing**
- **Router** - маршрутизация сообщений
- **Classifier** - классификация намерений (вопрос/запрос/координация)
- **Noise Filter** - отсечение флуда и нерелевантных сообщений
- **Transcriber** - преобразование голоса в текст (Whisper API)
- **Archiver** - сохранение всех сообщений для аудита

### 3. **AI/RAG Engine**
- **Embedding Model** - векторизация текстов
- **Vector Store** - Qdrant для хранения embeddings
- **Retrieval** - поиск релевантных фрагментов знаний
- **LLM** - генерация ответов (GPT-4, Claude)
- **Prompt Engineering** - шаблоны и контекст

### 4. **Knowledge Base**
Источники знаний:
- История решённых обращений (validated by humans)
- Карточки Kaiten с решениями
- Внутренняя и публичная документация
- FAQ и база знаний компании
- Транскрипты телефонных звонков через АТС МТС

### 5. **Integrations**
- **Kaiten API** - создание/обновление тикетов
- **Calendar APIs** - проверка доступности, бронирование
- **Task Trackers** - синхронизация задач
- **MTS Digital PBX API** - получение записей звонков, транскрипция, webhook уведомления
- **Internal Channels** - уведомления команды

### 6. **Data Layer**
- **PostgreSQL** - основная БД (диалоги, пользователи, метаданные)
- **Qdrant** - векторная БД для RAG
- **Redis** - кэш, очереди задач, rate limiting
- **Object Storage** - архивы, аудио файлы

## Потоки данных

### Входящее сообщение от пользователя

```
1. Telegram → Bot → Message Router
2. Router → Noise Filter (флуд?)
3. Router → Context Analyzer (о чём сообщение?)
4. Router → Intent Classifier (вопрос/запрос/координация?)
5. Classifier → AI Engine (если вопрос)
6. AI Engine → Vector Search (поиск в knowledge base)
7. AI Engine → LLM (генерация ответа)
8. LLM → Response + "Request Human?" button
9. Bot → Send to User
10. Archiver → Store in DB
```

### Эскалация к человеку

```
1. User → Clicks "Request Human" button
2. Bot → Create Kaiten ticket (with full context)
3. Bot → Notify internal team channel
4. Bot → Ack user: "Ваш запрос передан специалисту"
5. Specialist → Responds in Kaiten or Telegram
6. Bot → Relay response to User
7. Knowledge Base → Update with new validated answer
```

### Голосовое сообщение

```
1. User → Sends voice message
2. Bot → Download audio file
3. Bot → Transcribe with Whisper API
4. Bot → Process as text message (see above)
5. Bot → Store both audio + transcript
6. Bot → Send transcript back: "Вы сказали: ..."
```

### Телефонный звонок через АТС МТС

```
1. User → Calls support phone number (via MTS PBX)
2. MTS API → Webhook notification to Bot (call started/ended)
3. Bot → Fetch call recording + metadata from MTS API
4. Bot → Transcribe call with Whisper API
5. Bot → Classify call content (question/issue/request)
6. Bot → Extract key topics and questions
7. Bot → Store in PostgreSQL + vectorize for Qdrant
8. Bot → Match with existing customer/tickets
9. Bot → Create Kaiten ticket if needed
10. Bot → Notify team channel with call summary
11. Knowledge Base → Call transcript available for RAG
```

## Принципы проектирования

### Асинхронность
- Все IO операции асинхронные (async/await)
- Очереди задач через Redis/Celery
- Non-blocking ответы пользователям

### Масштабируемость
- Stateless bot instances (можно запустить несколько)
- Shared state в Redis
- Горизонтальное масштабирование через load balancer

### Отказоустойчивость
- Retry механизмы для API вызовов
- Circuit breakers для внешних сервисов
- Graceful degradation (fallback на упрощённые ответы)
- Health checks и мониторинг

### Безопасность
- Все секреты в переменных окружения
- Шифрование чувствительных данных в БД
- Аудит логи всех действий
- Rate limiting на API endpoints

## Технологический стек

### Backend
- **Python 3.11+**
- **python-telegram-bot** - Telegram Bot framework
- **FastAPI** - REST API (опционально)
- **SQLAlchemy** - ORM
- **Alembic** - миграции БД

### AI/ML
- **OpenAI API** - GPT-4 для генерации
- **Anthropic Claude** - альтернативный LLM
- **Qdrant** - векторная БД
- **LangChain** - RAG pipeline
- **Whisper** - speech-to-text

### Storage
- **PostgreSQL** - основная БД
- **Redis** - кэш и очереди
- **Qdrant** - векторная БД
- **MinIO/S3** - файловое хранилище

### Infrastructure
- **Docker** - контейнеризация
- **Docker Compose** - локальная разработка
- **Kubernetes** - продакшен (опционально)
- **GitHub Actions** - CI/CD
- **Prometheus + Grafana** - мониторинг

## Следующие шаги

- [Компоненты](./components.md) - детальное описание модулей
- [База данных](./database.md) - схема и модели
- [API Reference](../api/) - документация API

---

**Версия:** 1.0  
**Автор:** Dialog Keeper Team  
**Дата:** 2026-01-08

